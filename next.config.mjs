/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
        domains: ['yt3.ggpht.com', 'i.ytimg.com', 'www.googleapis.com'],
    },
};

export default nextConfig;